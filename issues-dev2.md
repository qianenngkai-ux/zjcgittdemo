# 代码问题清单 —— dev2 分支

分析对象：`dev2` 分支（相对 `dev` 的改动 + 排序算法模块整体）
分析日期：2026-09-16
对比基线：`dev`（`0cab51a`）

结论速览：**没有发现会导致崩溃或结果错误的功能缺陷** —— 6 个算法在 252 组模糊测试下结果全部正确。但发现 **2 项中等严重度的测试盲区**：代码里声明的两条契约（可比较性、稳定性）**没有任何测试覆盖**，因此其中的不准确之处完全不可见。

> 注：本文件是 `dev2` 分支的分析。`dev` 分支的分析与修复记录在 `issues.md`，两份独立保留。

> ## 修复状态（2026-09-16 更新）
>
> | # | 问题 | 状态 | 修复方式 |
> |---|---|---|---|
> | 1 | 可比较性契约描述不准确 | ✅ 已修复 | docstring 改为准确表述，并列出各算法实际用到的运算符；新增 `Ranked` 自定义类型与 2 条契约用例 |
> | 2 | 稳定性声明零测试覆盖 | ✅ 已修复 | 新增 `StableSortMixin`，3 条用例覆盖三个稳定算法；三个变异（插入/归并/冒泡）现已全部被抓住 |
> | 3 | `selection_sort` 无早退 | ✅ 已修复 | docstring 补上该代价与实测数据 |
> | 4 | `issues.md` 与当前分支不符 | ✅ 已修复 | 在 `issues.md` 顶部加适用范围说明并链接到本文件 |
>
> 修复后验证：Python **135** 项、JS **35** 项、渲染检查 **21** 项，全部通过。
> 测试数从 114 增至 135 = 通用契约 21×6 + 稳定性契约 3×3。
>
> **修复过程中发现并修正了我自己的一个错误**：编辑测试文件时误将 5 条大规模回归用例
> （含快排崩溃的三条 `test_large_*`）挪进了 `StableSortMixin`，导致它们只对稳定算法运行、
> **不再覆盖 `quick_sort`**。已移回 `SortContractMixin`，`TestQuickSort` 恢复为 21 条。

| # | 严重程度 | 问题 | 位置 |
|---|---|---|---|
| 1 | 🟠 中 | 可比较性契约描述不准确，且无测试覆盖 | `algorithms/sort.py` docstring |
| 2 | 🟠 中 | 稳定性声明零测试覆盖 | `algorithms/sort.py` + `README.md` |
| 3 | 🟡 低 | `selection_sort` 无早退，有序输入比同属 O(n²) 的另两个慢 3000+ 倍 | `algorithms/sort.py` |
| 4 | 🟡 低 | 仓库中的 `issues.md` 与当前分支不符 | `issues.md` |

---

## 1. 🟠 可比较性契约描述不准确

**位置**：`algorithms/sort.py` 模块 docstring

**声称**：

> 只要求元素之间可比较（`<` / `<=`），因此数字、字符串都适用

**实际**：六个算法用到的运算符各不相同，没有任何单一运算符能满足全部。

实测结果：

| 只提供该运算符 | 可用 | 失败 |
|---|---|---|
| `__lt__` | bubble / insertion / selection | **merge / quick / heap** |
| `__le__` | merge | **其余 5 个** |

从源码提取的各算法实际用到的运算符：

```
bubble_sort        用到: >
insertion_sort     用到: >, >=
selection_sort     用到: <, >
merge_sort         用到: <=, >
quick_sort         用到: >          （_partition 额外用 < 与 <=）
heap_sort          用到: >          （_sift_down 额外用 < 与 >=）
```

**为什么只定义 `__lt__` 时仍有 3 个能用**：Python 的反射比较会让 `a > b` 退回到 `b.__lt__(a)`，所以 bubble / insertion / selection 能工作。但 `a <= b` 会反射成 `b >= a`，而 `>=` 无法再退化到 `__lt__`，因此 merge 和 quick 必须有显式的 `__le__`。

**测试为何没发现**：114 项测试全部使用 `int` / `float` / `str`，它们天然支持全部运算符，这个不一致完全不可见。

**复现**：

```python
class OnlyLt:
    def __init__(self, v): self.v = v
    def __lt__(self, o): return self.v < o.v

from algorithms.sort import merge_sort, quick_sort, heap_sort
merge_sort([OnlyLt(3), OnlyLt(1)])   # TypeError: '<=' not supported
quick_sort([OnlyLt(3), OnlyLt(1)])   # TypeError: '<=' not supported
heap_sort([OnlyLt(3), OnlyLt(1)])    # TypeError: '>=' not supported
```

**建议修复**：

1. 把 docstring 改成准确的表述，例如「要求元素支持完整的大小比较（`<` `<=` `>` `>=`），即具备全序关系」
2. 补一条使用自定义可比较类型的测试，把契约固化下来 —— 这同时也能覆盖问题 2

---

## 2. 🟠 稳定性声明零测试覆盖

**位置**：`algorithms/sort.py` 各算法 docstring + `README.md` 的「稳定」列

六个算法都明确声明了稳定性，`README.md` 甚至专门有一列。实测这些声明**全部属实**：

```
bubble_sort     声称 稳定    实测 稳定    ✅
insertion_sort  声称 稳定    实测 稳定    ✅
selection_sort  声称 非稳定  实测 非稳定  ✅
merge_sort      声称 稳定    实测 稳定    ✅
quick_sort      声称 非稳定  实测 非稳定  ✅
heap_sort       声称 非稳定  实测 非稳定  ✅
```

**但没有任何测试验证这一点。** 稳定性只有在「元素可比较但彼此不相等」时才可观测，而全部 114 项测试只使用 int / float / str，无法构造这种场景。

**变异验证**：把 `insertion_sort` 的 `result[j] > key` 改成 `result[j] >= key`

```python
# 排序结果仍然正确（数字场景下不可观测）
# 但稳定性被破坏：
[Item(1,0), Item(1,1), Item(1,2)]  →  [(1#2), (1#1), (1#0)]   完全颠倒
# 而 114 项测试全部通过
```

这意味着：**任何人无意中把 `>` 改成 `>=`，CI 都不会报警**，而文档承诺的稳定性已经失效。

**建议修复**：加一组稳定性测试。思路是用只按 key 比较、带原始序号的对象，断言相同 key 的元素保持原有相对顺序：

```python
@total_ordering
class Keyed:
    def __init__(self, key, order): self.key, self.order = key, order
    def __eq__(self, o): return self.key == o.key
    def __lt__(self, o): return self.key < o.key

def test_preserves_order_of_equal_elements(self):
    items = [Keyed(1, 0), Keyed(1, 1), Keyed(1, 2)]
    out = self.sort(items)
    self.assertEqual([x.order for x in out], [0, 1, 2])
```

注意：该测试需要元素支持完整比较运算符，与问题 1 是同一件事 —— 一并处理即可。对声明「非稳定」的三个算法，则不应断言顺序保持（非稳定只表示不保证，不表示一定打乱），可改为只断言排序结果正确。

---

## 3. 🟡 `selection_sort` 无早退，已排序输入比同属 O(n²) 的另两个慢 3000+ 倍

**位置**：`algorithms/sort.py` — `selection_sort`

**现象**：对已经有序的输入，选择排序明显比其他 O(n²) 算法慢得多。

实测（20000 个**已排序**元素，各跑 5 次取最好成绩，`time.perf_counter`）：

| 算法 | 耗时 | 相对 `insertion_sort` |
|---|---|---|
| `bubble_sort` | 0.504 ms | 0.5x |
| `insertion_sort` | 1.006 ms | 1.0x |
| `quick_sort` | 10.109 ms | 10.0x |
| `merge_sort` | 15.777 ms | 15.7x |
| `heap_sort` | 23.341 ms | 23.2x |
| **`selection_sort`** | **3388.730 ms** | **3367.8x** |

这张表里有两个反直觉之处：

**其一，选择排序比同属 O(n²) 的另外两个慢三千多倍。** 原因是它每一轮都必须完整扫描未排序区才能确定最小值，无法利用「已经有序」提前退出。bubble 和 insertion 都实现了早退 —— bubble 靠 `swapped` 标志，insertion 靠内层 `while` 不进入，因此两者在有序输入上退化为 O(n)。

**其二，三个 O(n log n) 算法反而比两个 O(n²) 算法慢 10–23 倍。** 因为归并/快排/堆排没有早退机制，无论输入如何都要做完 O(n log n) 的工作。这正说明「渐进复杂度更优」不等于「任何输入下都更快」——对近乎有序的数据，带早退的 O(n²) 算法反而更合适。模块 docstring 的「选择建议」里「基本有序 → 插入排序」这条是对的。

**这不是缺陷** —— 是选择排序的固有性质。但它与另外两个 O(n²) 算法的行为差异很大，而 docstring 只说了优势（「交换次数固定为 O(n)」），没提这个代价。只看到复杂度表里的「O(n²)」一行，容易误以为三者在有序输入上表现相近。

模块的「选择建议」里写了「n 很小或基本有序 → 插入排序」，间接覆盖了这一点。

**建议修复**：在 `selection_sort` 的 docstring 补一句：

> 注意：无论输入是否有序，比较次数恒为 n(n−1)/2，没有提前退出机制。

---

## 4. 🟡 仓库中的 `issues.md` 与当前分支不符

**位置**：`issues.md`

该文件标题写明「分析对象：`dev` 分支」，记录的 9 项问题与修复也都是针对 `dev` 的。而 `dev2` 包含 `dev` 的全部内容外加三种新算法，因此在 `dev2` 上这份文档描述的不是当前代码状态。

**处理**：本次分析输出到 `issues-dev2.md`，原 `issues.md` 保留作为 `dev` 分支的历史记录。

---

## 已验证无问题的部分

以下经过实际执行验证，未发现缺陷，记录在此以免重复排查。

**复杂度声明** —— 用规模翻倍法实测增长趋势：

| 算法 | 声称 | n=2000 | n=4000 | n=8000 | 倍率 | 判定 |
|---|---|---|---|---|---|---|
| bubble | O(n²) | 0.0978s | 0.3477s | — | 3.56x | ✅ |
| insertion | O(n²) | 0.0345s | 0.1541s | — | 4.46x | ✅ |
| selection | O(n²) | 0.0358s | 0.1430s | — | 3.99x | ✅ |
| merge | O(n log n) | 0.0019s | 0.0042s | 0.0088s | 2.10x | ✅ |
| quick | O(n log n) | 0.0011s | 0.0024s | 0.0048s | 2.02x | ✅ |
| heap | O(n log n) | 0.0017s | 0.0042s | 0.0085s | 2.04x | ✅ |

O(n²) 类每次翻倍约 4x、O(n log n) 类约 2.1x，与声明一致。

**堆排序最坏情况** —— 逆序输入 100000 个 0.131s、400000 个 0.595s，结果正确且不崩溃，证实最坏也是 O(n log n)，且迭代式 `_sift_down` 没有递归深度风险。

**算法正确性** —— 模糊测试 252 组比对（6 种输入形态：随机 / 近序 / 逆序 / 重复多 / 浮点 / 单值 × 7 种规模 × 6 个算法），结果与 Python 内置 `sorted()` **全部一致**，且**均未修改入参**。

**稳定性声明属实** —— 见问题 2 的实测表格，6 个算法的声明全部与实测相符。

**字符串支持** —— 6 个算法对字符串数组（含大小写混合与中文）排序结果均与 `sorted()` 一致。

**其他文档声明**：

- `selection_sort`「交换次数固定为 O(n)」：实测 n=10 交换 9 次、n=100 交换 95 次、n=1000 交换 994 次，均 ≤ n−1 ✅
- `insertion_sort`「近乎有序时接近 O(n)」：5000 元素，基本有序 0.007s vs 完全随机 0.238s，约 **34 倍**差距 ✅

**文档一致性** —— `README.md` 与模块 docstring 的复杂度表**逐行一致**（6 行全部对应）。`doctest` 覆盖 6 个算法，6 项全部通过。

**变异测试** —— 插入排序放错位置、选择排序漏扫一个元素、堆排序选错孩子，三类缺陷**全部被测试抓住**。

**其他模块未受影响** —— JS 单元测试 35/35、无头 Chrome 渲染检查 21/21 全部通过。

---

## 附：验证方式

所有结论均来自可复现的实际执行，而非静态阅读：

- 可比较性问题由构造只定义 `__lt__` / `__le__` 的类逐个调用 6 个算法得出
- 稳定性问题由变异测试（`>` 改 `>=`）配合带序号的自定义类型确认
- selection_sort 耗时由已排序 20000 元素实跑计时
- 复杂度声明由规模翻倍法测增长倍率
- 稳定性声明由 200 轮随机重复值输入下的自定义类型实测

补充说明：分析过程中我的临时探针脚本出过一次错（自定义类型缺 `__ge__` 导致 `TypeError`，而非算法问题），已补全比较运算符后重跑。上表结论取自修正后的结果。
